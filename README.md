# text_classification
Text classification for e-commerce items using fastText library. fastText is a word embedding technique similar to word2vec with one key difference. It uses character n grams instead of words to train a neural network to produce word embeddings or word vectors. 
